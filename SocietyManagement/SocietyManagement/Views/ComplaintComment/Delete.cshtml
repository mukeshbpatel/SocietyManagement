@model SocietyManagement.Models.ComplaintComment

@{
    ViewBag.Title = "Complaint Comment";
}

<h3><span class="glyphicon glyphicon-hand-up"></span> Complaint Comment</h3>

<h4>Are you sure you want to delete this?</h4>
<div>

    <hr />
    <dl class="dl-horizontal">
       
        <dt>
            @Html.DisplayNameFor(model => model.Complaint.Title)
        </dt>
         <dd>
            @Html.DisplayFor(model => model.Complaint.Title)
        </dd>        
        <dt>
            @Html.DisplayNameFor(model => model.AssignToID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AssignTo.Name)
        </dd>

        
        <dt>
            @Html.DisplayNameFor(model => model.StatusID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Status.KeyValues)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Comment)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Comment)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            <div class="form-group">
                <div class="form-actions no-color col-md-offset-1 col-md-11">
                    <button type="submit" class="btn btn-danger"><i class="fa fa-remove"></i> Delete</button>
                    <a href="@Url.Action("Details","Complaint",new { id = Model.ComplaintID})" class="btn btn-success" role="button">Cancel</a>
                </div>
                </div>
            </div>
                }
            </div>
